import * as React from "react";
import * as ReactDOM from "react-dom";

import { Datepicker } from "./components/datepicker";

ReactDOM.render(
    <Datepicker compiler="TypeScript" framework="React" />,
    document.getElementById("example")
);